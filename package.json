{
  "name": "it-helpdesk",
  "version": "1.0.0",
  "description": "IT Helpdesk Application",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build && npx prisma generate",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}

